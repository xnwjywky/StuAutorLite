# 前端环境配置示例 — 复制为 .env 并按需修改
# 注意：Vite 环境变量必须以 VITE_ 前缀开头才会注入前端代码

# API 基础地址。留空 = 自动检测（优先当前页面 IP:8000，失败回退 Vite proxy）
# 局域网部署建议留空（走 Vite 代理，无需 CORS 放行）
VITE_API_BASE_URL=

# 后端共享访问密钥（与 backend/.env 的 APP_KEY 保持一致）。
# ⚠️ 若后端配置了 APP_KEY（生产/局域网部署建议启用鉴权），
#    前端必须配置相同的 VITE_APP_KEY，否则除 /health 外的所有 /api 接口返回 401。
# 后端未启用鉴权时保持为空即可。
# VITE_APP_KEY=
